Flask>=2.0.0
flask-cors>=3.0.10
requests>=2.25.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.23.0
torch>=2.0.0
rasterio>=1.3.0
geopandas>=0.14.0
shapely>=2.0.0
scipy>=1.10.0
# 以下两项后端无直接 import，但为必需：
#   openpyxl       —— alert.py 的 pd.read_excel() 读 .xlsx 时由 pandas 调用，缺失会报 ImportError
#   python-dateutil —— pandas 的硬依赖
openpyxl>=3.1.0
python-dateutil>=2.8.2
# matplotlib / tqdm 后端未直接引用（仅 Training 脚本使用），此处保留以与根 requirements.txt 对齐
matplotlib>=3.7.0
tqdm>=4.65.0